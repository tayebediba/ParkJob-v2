import { createTheme } from "@material-ui/core";

// Create a theme instance.
export const theme = createTheme({
  direction: "rtl",

  typography: {
    fontFamily: " Dana ",
  },
  palette: {
    primary: {
      main: "#1E4383",
    },
    secondary: {
      main: "#5243ac",
    },
  },
});
